Todays Plan

1) Finish "Add New Spot" card

2) Finish "Spot Detail" card 

3) Side menu (hamburger) — ❌ not built. Spec needs a full slide-in panel with two states: signed-in (avatar, name/handle, My profile/My spots/Saved spots/Notifications/Settings/Sign out) and signed-out (email/password sign-in form, Google button, create-account link)

4) Fix Data shape gap: the design's spot objects carry a difficulty field that src/types/spot.ts's Spot type doesn't have yet — worth adding since you're already building a Difficulty selector.
     
     Does it make sense to have the SpotData interface in page

